apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: standup-bot-ingress
spec:
  tls:
    - hosts:
        - standup.botenza.com
      secretName: standup-bot-cert
  rules:
    - host: standup.botenza.com
      http:
        paths:
          - path: /api/slack
            pathType: "Prefix"
            backend:
              service:
                name: standup-api-service
                port:
                  number: 3000
          - path: /
            pathType: "Prefix"
            backend:
              service:
                name: standup-ui-service
                port:
                  number: 3000