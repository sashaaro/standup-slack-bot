extends layout
block dashboard_content
    .ui.items
        each user in users
            .item
                .image
                    img(src= user.profile.image_72).img-thumbnail
                .content
                    .header= user.profile.real_name + ' (' + user.name + ')'
                    each question in user.questions
                        .description
                            div= questionList[question.index]
                            div= question.answer

    .six.wide.right.floated.column
        h4= 'For 4 october'
